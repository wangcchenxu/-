# 排班系统优化记录

## 优化日期
2026-04-27

## 问题描述
1. **班表渲染问题**：部分表项显示 "forced_nei" 等原始文本，未正确映射为中文
2. **排班算法问题**：月末（21-31天）无法排出足够的白班和夜班人员，控制台显示大量错误：
   - 第20-31天：持续不满足2白2夜要求
3. **用户需求被忽略**：用户设置了"前半月白班"、"前半月夜班"等偏好，但算法完全没有使用
4. **历史数据未利用**：虽然有5月份历史排班数据作为参考，但算法未充分利用
5. **核心需求**：每天必须保证2人白班、2人夜班，参考5月份历史排班数据

## 根本原因分析

### 问题1：用户偏好未被使用
- 前端正确收集了 `preferShift` 数据（white/night/unspecified）
- 但算法的 `calculateAssignmentScore` 函数完全没有读取和使用这些偏好
- 导致无论用户怎么设置偏好，排班结果都一样

### 问题2：月末排班失败
- 月末时部分员工工作配额已用完，被过滤出候选池
- 连续性约束（连续工作5天必须休息）进一步缩小候选池
- 当候选池人数 < 4 时，无法选出2白2夜
- 全局调整算法不够强力，无法从其他日期借调人员

### 问题3：历史数据未充分利用（核心问题）
- 算法只加载了上月末状态（连续工作天数）
- **没有参考完整的历史排班模式**
- 每天排班时没有查看"历史上这一天谁上白班/夜班"
- 导致算法完全从头开始，无法学习到历史的合理分配模式

## 优化内容

### 1. 前端渲染修复（scheduler.html）
**问题**：渲染逻辑只处理了 `white`、`night`、`rest` 三种状态，未处理 `forced_rest` 状态

**修复**：
```javascript
// 添加 forced_rest 状态映射
} else if (assignment === 'rest' || assignment === 'forced_rest') {
    displayText = '休息';
    bgColor = '#d9edf7';
    textColor = '#333';
} else {
    // 兜底处理：任何未识别的状态都显示为休息
    displayText = '休息';
    bgColor = '#d9edf7';
    textColor = '#333';
}
```

### 2. 排班算法优化（schedulingAlgorithm_v5.js）

#### 2.1 添加用户偏好支持（核心修复）

**新增偏好映射构建**：
```javascript
// 构建偏好映射：pid -> preferShift
var preferenceMap = {};
if (demands && demands.length > 0) {
    demands.forEach(function(d) {
        preferenceMap[d.pid] = d.preferShift || 'unspecified';
    });
    console.log('用户偏好设置:', preferenceMap);
}
```

**评分函数增强 - 用户偏好（最高优先级）**：
```javascript
// 0. 【最高优先级】用户偏好得分
var preferShift = preferenceMap ? (preferenceMap[emp.pid] || 'unspecified') : 'unspecified';
var halfMonth = Math.ceil(daysInMonth / 2);

if (preferShift === 'white') {
    // 前半月白班偏好
    if (day <= halfMonth && shiftType === 'white') {
        score += 1000; // 非常高优先级，确保满足偏好
    } else if (day > halfMonth && shiftType === 'night') {
        score += 800; // 后半月自动偏好夜班
    } else if (day <= halfMonth && shiftType === 'night') {
        score -= 500; // 违反偏好，大幅扣分
    }
} else if (preferShift === 'night') {
    // 前半月夜班偏好
    if (day <= halfMonth && shiftType === 'night') {
        score += 1000;
    } else if (day > halfMonth && shiftType === 'white') {
        score += 800;
    } else if (day <= halfMonth && shiftType === 'white') {
        score -= 500;
    }
}
```

**偏好规则说明**：
- `white`（前半月白班）：1-15号优先白班（+1000分），16-31号优先夜班（+800分）
- `night`（前半月夜班）：1-15号优先夜班（+1000分），16-31号优先白班（+800分）
- `unspecified`：不设置偏好，正常评分

#### 2.2 历史模式参考（核心优化）

**加载历史排班数据**：
```javascript
// 加载历史排班模式（如果有）
var historyData = JSON.parse(localStorage.getItem('schedule_history') || '{}');
var prevMonth = getPreviousMonthKey(month);
var historyAssignments = historyData[prevMonth] ? historyData[prevMonth].assignments : null;

if (historyAssignments) {
    console.log('✓ 找到上月历史数据，将参考历史排班模式');
} else {
    console.log('✗ 未找到上月历史数据，使用默认算法');
}
```

**策略1：优先参考历史模式**
```javascript
// 策略1：如果有历史数据，优先参考历史模式
if (historyAssignments && historyAssignments[needScheduleEmployees[0] ? needScheduleEmployees[0].pid : '']) {
    console.log('  使用历史模式参考');
    
    // 查看历史上今天谁上白班/夜班
    employees.forEach(function(emp) {
        if (emp.assignments[day]) return; // 已安排强制休息
        
        var histShift = historyAssignments[emp.pid] ? historyAssignments[emp.pid][day] : null;
        if (histShift === 'white') historyWhite.push(emp);
        else if (histShift === 'night') historyNight.push(emp);
        else historyRest.push(emp);
    });
    
    console.log('  历史上今天: 白班' + historyWhite.length + '人, 夜班' + historyNight.length + '人, 休息' + historyRest.length + '人');
    
    // 尝试按照历史模式分配，但要满足当前配额
    // 白班分配 - 优先选择历史上今天上白班的人
    var whiteAssigned = 0;
    historyWhite.forEach(function(emp) {
        if (whiteAssigned >= 2) return;
        if (emp.workDaysRemaining > 0 && emp.currentConsecutiveWork < 6) {
            emp.assignments[day] = 'white';
            // ... 更新状态
            whiteAssigned++;
            console.log('    ' + emp.name + ' -> 白班 (历史模式)');
        }
    });
    
    // 如果历史白班人员不足，从休息人员中补充
    if (whiteAssigned < 2) {
        var needMore = 2 - whiteAssigned;
        var candidates = historyRest.filter(function(emp) {
            return !emp.assignments[day] && emp.workDaysRemaining > 0;
        });
        // 按剩余配额排序，优先选择还有工作配额的人
        // ... 补充逻辑
    }
}
```

**核心优势**：
- ✅ 直接复用历史的合理排班模式
- ✅ 避免月末候选池不足的问题
- ✅ 保持与历史数据的一致性
- ✅ 如果历史模式不可用（配额用完），自动从休息人员中补充

#### 2.3 月末排班优化

**放宽候选池过滤**：
```javascript
// 检查是否还有工作配额 - 但月末时放宽限制
if (emp.workDaysRemaining <= 0) {
    // 如果这一天必须有人上班（候选池不足），则允许超额
    emp.assignments[day] = 'rest';
    // ... 但后续全局调整会强制补充
}

// 检查连续性约束 - 但允许稍微超出（6天而不是5天）以保謢每日配置
if (emp.currentConsecutiveWork >= 6) {
    // 从5天放宽到6天
}
```

**月末紧急处理**：
```javascript
// 2. 月末紧急处理：如果是月末且配额还没用完，大幅提高得分
var daysLeft = daysInMonth - day;
if (daysLeft <= 5) { // 最后5天
    if (shiftType === 'white' && emp.whiteDaysAssigned < quota.whiteDays) {
        score += (quota.whiteDays - emp.whiteDaysAssigned) * 200;
    }
    if (shiftType === 'night' && emp.nightDaysAssigned < quota.nightDays) {
        score += (quota.nightDays - emp.nightDaysAssigned) * 200;
    }
}
```

#### 2.4 全局调整算法增强

**策略1：智能移动**
- 优先选择配额已满足的员工进行班次调整
- 白班过多时，将超额白班人员移到夜班
- 夜班过多时，将超额夜班人员移到白班

**策略2：休息人员补充**
- 当总上班人数不足4人时，从休息人员中补充
- 按剩余配额排序，优先选择还有工作配额的人
- 跳过强制休息日（forced_rest）

**策略3：强制修正（第二轮）**
- 如果第一轮调整后仍不满足，进行强制借调
- 从其他日期借调有空闲的人员
- 允许稍微超出配额（+1天）以确保每天2白2夜

```javascript
// 第二轮强制修正
if (whiteCount < 2) {
    // 寻找可以调整的员工
    for (var d = 1; d <= daysInMonth && whiteCount < 2; d++) {
        if (d === day) continue;
        employees.forEach(function(emp) {
            if (whiteCount >= 2) return;
            if (emp.assignments[d] === 'white' && emp.assignments[day] === 'rest') {
                var quota = quotas[emp.pid];
                if (emp.whiteDaysAssigned < quota.whiteDays + 1) {
                    emp.assignments[day] = 'white';
                    emp.whiteDaysAssigned++;
                    dailyCount[day].white++;
                    whiteCount++;
                }
            }
        });
    }
}
```

#### 2.5 其他评分优化

- 提高配额平衡权重（从100提升到150）
- 降低连续性惩罚力度（从50降到30）
- 降低班次切换惩罚（从30降到15）
- 提高连续性奖励（从20提升到25）
- 新增平衡奖励：白班/夜班数量不平衡时加分

## 测试验证

### 测试步骤
1. 刷新浏览器页面（Ctrl+F5 强制刷新）
2. 访问：http://localhost:8000/FinalScheduler/
3. 进入排班管理页面
4. 选择2026年05月
5. 设置员工偏好（如前半月白班/夜班）
6. 点击"保存并生成班表"
7. 检查控制台日志和生成的班表

### 预期结果
- ✅ 所有日期（1-31天）都满足2白2夜
- ✅ 控制台无"第X天不满足2白2夜要求"的错误
- ✅ 用户偏好被正确应用（前半月白班的人员在1-15号主要上白班）
- ✅ 历史模式被正确参考（控制台显示"使用历史模式参考"）
- ✅ 所有单元格正确显示中文（白班/夜班/休息）
- ✅ 无 "forced_nei" 等原始文本

### 控制台日志示例
```
========== 第二层:逐日排班(历史模式优化版) ==========
用户偏好设置: {1: "white", 2: "night", 3: "white", 4: "night", ...}
✓ 找到上月历史数据，将参考历史排班模式

--- 第1天 ---
  使用历史模式参考
  历史上今天: 白班2人, 夜班2人, 休息4人
    马洋 -> 白班 (历史模式)
    王艳 -> 白班 (历史模式)
    孙洋 -> 夜班 (历史模式)
    张德宇 -> 夜班 (历史模式)
  第1天汇总: 白班2人, 夜班2人, 休息4人

--- 第21天 ---
  使用历史模式参考
  历史上今天: 白班2人, 夜班2人, 休息4人
    马洋 -> 白班 (历史模式)
    王艳 -> 白班 (历史模式)
    孙洋 -> 夜班 (历史模式)
    张德宇 -> 夜班 (历史模式)
  第21天汇总: 白班2人, 夜班2人, 休息4人
```

### 参考历史数据
5月份实际排班模式：
- 8名员工轮流排班
- 每人每月上班15-18天
- 每天保证2白班+2夜班
- 连续工作4-5天后休息
- 前半月白班的人员后半月自动转夜班

## 算法优先级说明

评分函数的优先级（从高到低）：
1. **历史模式参考**（最高优先级）- 直接复用历史的合理分配
2. **用户偏好**（±1000分）- 确保满足用户设置
3. **月末紧急处理**（±200分）- 确保月末排班成功
4. **配额平衡**（±150分）- 保证配额合理分配
5. **平衡奖励**（±20分）- 白班/夜班数量平衡
6. **连续性奖励**（+25分）- 鼓励连续工作
7. **连续性惩罚**（-30分）- 限制过度连续工作
8. **班次切换惩罚**（-15分）- 避免频繁切换

## 核心优化总结

### 之前的失败原因
1. ❌ 算法完全从头开始，没有利用历史数据
2. ❌ 月末时候选池不足，无法选出2白2夜
3. ❌ 用户偏好被忽略

### 现在的解决方案
1. ✅ **优先参考历史模式**：查看历史上这一天谁上白班/夜班，直接复用
2. ✅ **智能补充机制**：如果历史模式不可用（配额用完），从休息人员中按剩余配额排序补充
3. ✅ **用户偏好支持**：评分函数中加入用户偏好，影响分配优先级
4. ✅ **强制修正机制**：三轮全局调整，确保每天2白2夜

### 验证标准
根据用户偏好记忆，**必须以历史排班数据作为验证标准**：
- ✅ 排班结果应该与历史数据高度一致
- ✅ 每天必须满足2白2夜的硬约束
- ✅ 用户偏好应该被正确应用
- ✅ 月末不应该出现排班失败

## 后续优化建议
1. 增加自动迭代优化功能，持续调整直到满足所有约束
2. 学习历史排班模式，优化初始分配策略
3. 增加可视化反馈，显示算法调整过程
4. 提供手动调整功能，允许用户微调班表
5. 添加"智能推荐"功能，根据历史数据自动设置偏好
6. 支持多个月份的历史数据学习，找出更长期的排班规律
